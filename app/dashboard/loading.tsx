"use client";

import { RouteLoading } from "@/components/route-loading";

// Displayed automatically by Next.js during dashboard route transitions/data fetch.
export default function Loading() {
  return <RouteLoading />;
}
